/**
 * Placeholder de servidor/proxy MCP para WordPress.
 *
 * Este archivo se incluye como referencia para que el ejemplo
 * `mcp/mcp.config.example.json` no apunte a una ruta inexistente.
 *
 * Implementacion futura:
 * - Exponer herramientas para consultar endpoints WP REST.
 * - Autenticacion con Application Passwords.
 * - Operaciones de lectura/escritura segun permisos.
 */

console.log(
  "mcp-wordpress-proxy placeholder. Implementa aqui el servidor MCP real para WordPress."
);
